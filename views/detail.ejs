<!DOCTYPE html>
<html>
<head>
  <title><%= house.name %> - Details</title>
  <style>
    body {
      background-color: #0a0a14;
      color: white;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 700px;
      margin: 40px auto;
      padding: 20px;
      background-color: #121221;
      border-radius: 12px;
      box-shadow: 0 0 12px rgba(0, 0, 0, 0.4);
    }

    .house-image {
      width: 100%;
      max-height: 400px;
      object-fit: cover;
      border-radius: 8px;
      margin-bottom: 20px;
    }

    .detail-title {
      font-size: 28px;
      margin-bottom: 10px;
    }

    .detail-info {
      margin: 10px 0;
      line-height: 1.6;
    }

    .label {
      font-weight: bold;
      color: #aaa;
    }

    .back-link {
      display: block;
      margin-top: 30px;
      color: #ddd;
      text-align: center;
      text-decoration: none;
    }

    .back-link:hover {
      color: #1e90ff;
    }
  </style>
</head>
<body>
  <div class="container">
    <img src="<%= house.imageUrl %>" alt="House Image" class="house-image">

    <div class="detail-title"><%= house.name %></div>

    <div class="detail-info"><span class="label">Location:</span> <%= house.location %></div>
    <div class="detail-info"><span class="label">Price:</span> $<%= house.price %></div>
    <div class="detail-info"><span class="label">Description:</span> <%= house.description %></div>
    <div class="detail-info"><span class="label">Amenities:</span> <%= house.amenities.join(", ") %></div>
    <div class="detail-info"><span class="label">Contact Info:</span> <%= house.contact %></div>

    <a href="/" class="back-link">← Back to Listings</a>
  </div>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= house.name %> - Details</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header>
    <h1><%= house.name %></h1>
    <a href="/" class="back-link">← Back to Listings</a>
  </header>

  <main class="detail-container">
    <div class="detail-image">
      <img src="<%= house.imageUrl %>" alt="House Image">
    </div>

    <div class="detail-info">
      <p><strong>Location:</strong> <%= house.location %></p>
      <p><strong>Price:</strong> $<%= house.price %></p>
      <p><strong>Description:</strong> <%= house.description || "No description provided." %></p>

      <p><strong>Amenities:</strong> <%= house.amenities || "None listed." %></p>
      <p><strong>Contact:</strong> <%= house.contact || "Not provided" %></p>
    </div>

    <% if (house.location) { %>
    <div class="map-container">
      <iframe
        width="100%"
        height="300"
        style="border:0; border-radius: 10px; margin-top: 1rem;"
        loading="lazy"
        allowfullscreen
        referrerpolicy="no-referrer-when-downgrade"
        src="https://www.google.com/maps?q=<%= encodeURIComponent(house.location) %>&output=embed">
      </iframe>
    </div>
    <% } %>
  </main>
</body>
</html>
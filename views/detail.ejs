<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= house.name %> - House Details</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f7f9fc;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 800px;
      margin: 40px auto;
      background: white;
      padding: 24px;
      border-radius: 12px;
      box-shadow: 0 0 12px rgba(0, 0, 0, 0.08);
    }

    img {
      width: 100%;
      border-radius: 10px;
      margin-bottom: 20px;
    }

    h2 {
      margin-bottom: 10px;
    }

    p {
      margin: 6px 0;
      font-size: 16px;
    }

    .label {
      font-weight: bold;
      color: #333;
    }

    a.back-link {
      display: inline-block;
      margin-top: 20px;
      text-decoration: none;
      color: #007bff;
    }

    a.back-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2><%= house.name %></h2>

    <img src="<%= house.image %>" alt="House image">

    <p><span class="label">Location:</span> <%= house.location %></p>
    <p><span class="label">Price:</span> <%= house.price %></p>
    <p><span class="label">Description:</span> <%= house.description %></p>
    <p><span class="label">Amenities:</span> <%= house.amenities %></p>
    <p><span class="label">Contact:</span> <%= house.contact %></p>

    <a href="/" class="back-link">&larr; Back to Listings</a>
  </div>
</body>
</html>